// Platzhalter, kein geliefertes Artefakt.
//
// Der Canvas verweist im <helmet>-Block auf diese Datei, keines der drei Handoff-Archive
// enthaelt sie. Gebraucht wird sie nicht: Modernist ist reines CSS ("plain CSS on plain
// HTML: no JavaScript, no build step", siehe styles.css), und die Canvas-Elemente
// x-dc, sc-if und sc-for bringt support.js selbst mit. Ohne diese Datei rendert der
// Prototyp vollstaendig — es bleibt nur ein 404 in der Konsole.
